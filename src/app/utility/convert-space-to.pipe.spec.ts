import { ConvertSpaceToPipe } from './convert-space-to.pipe';

describe('ConvertSpaceToPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertSpaceToPipe();
    expect(pipe).toBeTruthy();
  });
});
